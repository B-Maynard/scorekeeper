import React from "react";
import "../css/NavButton.css";

export default function NavButton(props) {
    return (
        <div>
            <i className="bi bi-arrow-left position-absolute start-0"></i>
        </div>
    );
}